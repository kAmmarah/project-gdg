{
    "name": "project-gdg-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "cd frontend && npm install && npm run build",
        "install-all": "npm install && cd api && npm install && cd ../frontend && npm install"
    }
}